//go:build bindings

package main

// generatingBindings is true when Wails runs the program to generate the
// JavaScript bindings; the backend (shell, config dir) must not start then.
const generatingBindings = true
